language: node_js
node_js:
  - 8.9
cache:
  directories:
    - node_modules
    - client/node_modules
    
script:
  - yarn && cd client && yarn
  - yarn build
  
deploy:
- provider: script
  script:
  - yarn deploy:qa
  on:
    branch: travis
